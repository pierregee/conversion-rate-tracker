{
  "name": "conversion-rate-tracker",
  "private": true,
  "scripts": {
    "install:all": "(cd apps/web && pnpm i) & (cd apps/api && pnpm i)",
    "dev": "(cd apps/web && pnpm run dev) & (cd apps/api && pnpm run dev)"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.1",
    "husky": "^9.1.6",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5"
  }
}
